# Module 07: Gate

Quality gate: generate a story, score it with an LLM, route to approve or reject. Demonstrates `.gate().onPass().onFail()`.

## Documentation

Full tutorial: https://lab.pollack.ai/docs/agent-workflow/tutorial

## Running

```bash
export OPENAI_API_KEY=sk-...
./mvnw exec:java -pl module-07-gate
```
